

function Pokemon ({props}) {

    return (
    
        
         <ul >
            <li >Name: {props.name}</li> <li > URL:{props.url}</li>
             
        </ul>

        );
    }
    
    export default Pokemon;
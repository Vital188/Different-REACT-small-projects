function Props ({text1, color, padding, backgroundColor}) {

    return (

    <h1 
    style={{
    color: color,
    fontSize: '15px',
    padding: padding,
    backgroundColor: backgroundColor
    }}>{text1}</h1>
)

}

export default Props
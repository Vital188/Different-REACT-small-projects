function Textas({one, two, color}) {

    return (
    <div>
    <h1 style={{
        color: color
    }}>{one}</h1>
    <h2 style={{
        color: color
    }}>{two}</h2>
    </div>
    )
}

export default Textas